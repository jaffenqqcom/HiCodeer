[package]
name = "command-executor"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Backend-agnostic remote command execution trait shared by the openeuler-agent and qemu-agent VM backends."

[lib]
name = "command_executor"
path = "src/lib.rs"

[dependencies]
log = "0.4"
smol = "2"
